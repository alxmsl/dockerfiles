# firebase-tools

[Firebase Tools](https://www.npmjs.com/package/firebase-tools) image.

## Evironment

- `FIRESTORE_PORT`
- `GCP_PROJECT`
- `PUBSUB_PORT` 
- `UI_PORT`

## Build

```
$ make image ${VERSION}
```
